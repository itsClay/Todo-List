const initialState = [];

const todosReducer = (state = initialState, action) => {
  switch (action.type) {
    // case RECEIVE_TODOS:
    //  return [...state, action.]
    // case RECEIVE_TODO:
    //
    default:
      return state;
  }
};


export default todosReducer;
